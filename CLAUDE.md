# CLAUDE.md

This file provides guidance to Claude Code when working with this repository.

## Credential & Identity Switching

This workspace uses automatic credential switching via direnv + 1Password CLI.
Tokens are injected per-directory — no plaintext secrets in dotfiles.

See `~/.claude/references/credential-switching-setup.md` for full setup details.
